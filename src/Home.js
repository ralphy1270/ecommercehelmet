import { Link } from "react-router-dom";
import { Fragment } from "react";

const Home = () => {
  return (
    <Fragment>
      <Link to="category">Cateogry</Link>
    </Fragment>
  );
};

export default Home;
